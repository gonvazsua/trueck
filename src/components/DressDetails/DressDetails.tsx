import React from 'react';
import Grid from "@material-ui/core/Grid";

const DressDetails = (): JSX.Element => {

    return (
        <Grid container spacing={3}>
            Dress details
        </Grid>
    );
};

export default DressDetails;